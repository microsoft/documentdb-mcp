[project]
name = "vcore-mcp"
version = "0.1.0"
description = "A vcore mcp server"
readme = "README.md"
requires-python = ">=3.10"
dependencies = [
    "mcp[cli]>=1.6.0",
    "pymongo>=4.12.0",
]
[build-system]
requires = ["hatchling"]
build-backend = "hatchling.build"